package org.example.stage_back.repository;

import org.example.stage_back.entities.TypeNavire;
import org.springframework.data.jpa.repository.JpaRepository;
 
public interface TypeNavireRepository extends JpaRepository<TypeNavire, Integer> {
} 